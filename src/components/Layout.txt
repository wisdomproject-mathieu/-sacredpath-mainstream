import React from "react";
import Header from "./Header";

interface LayoutProps {
  children: React.ReactNode;
  showHeader?: boolean;
}

export default function Layout({ children, showHeader = true }: LayoutProps) {
  return (
    <div className="w-full min-h-screen text-text">
      {/* 
        max-w-7xl keeps the content contained on ultra-wide monitors.
        px-6 gives it breathing room on smaller screens. 
      */}
      <div className="max-w-7xl mx-auto px-6 pb-20">
        {showHeader && <Header />}
        
        <main className="w-full animate-fade-in">
          {children}
        </main>
      </div>
    </div>
  );
}