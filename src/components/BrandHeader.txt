type BrandHeaderProps = {
  className?: string;
  variant?: "compact" | "wide";
};

export function BrandHeader({
  className = "",
  variant = "compact",
}: BrandHeaderProps) {
  const variantClassName =
    variant === "wide" ? "sp-brand-wide" : "sp-brand-compact";

  return (
    <div className={`sp-brand ${variantClassName} ${className}`}>
      <div className="sp-brand-logo-wrap">
        <img
          src="/sacred-path-mark.png"
          alt="Sacred Path"
          className="sp-brand-logo"
        />
      </div>

      <div>
        <div className="sp-brand-kicker">Sacred Path</div>
        <div className="sp-brand-title">For Couples</div>
        <p className="sp-brand-subtitle">
          Ancient wisdom for modern couples.
        </p>
      </div>
    </div>
  );
}